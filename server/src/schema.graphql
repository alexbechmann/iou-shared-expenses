type Transaction {
  _id: String
  name: String
  purchase: Purchase
  purchaseId: String
}

type Purchase {
  _id: String
  name: String
  transactions: [Transaction]
}

type Query {
  getTransactions: [Transaction]
  getPurchases: [Purchase]
}