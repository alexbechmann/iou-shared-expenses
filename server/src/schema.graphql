type Transaction {
  _id: String
  name: String
}

type Purchase {
  _id: String
  name: String
  transactions: [Transaction]
}

type Query {
  getTransactions: [Transaction]
  getPurchases: [Purchase]
}