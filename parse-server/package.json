{
  "name": "@iou/cloud-code",
  "scripts": {
    "lint": "tslint -p tslint.json",
    "build2": "tsc-bundle --project ./tsconfig.json",
    "start": "docker-compose up",
    "watch": "gulp watch",
    "build": "tsc && mkdir -p dist && browserify lib/main.js --exclude buffer --t babelify --o dist/main.js"
  },
  "dependencies": {
    "@iou/core": "file:../core",
    "parse": "^1.11.0"
  },
  "devDependencies": {
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-runtime": "^6.26.0",
    "babelify": "^8.0.0",
    "browserify": "^11.0.1",
    "npm-run-all": "^4.1.2",
    "rimraf": "^2.6.2",
    "tslint": "^5.8.0",
    "typescript": "^2.5.3",
    "babel-core": "^6.0.20",
    "babel-polyfill": "^6.0.16",
    "babel-preset-es2015": "^6.0.15",
    "babel-preset-stage-0": "^6.0.15"
  },
  "private": true
}
